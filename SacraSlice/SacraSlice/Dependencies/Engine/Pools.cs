namespace SacraSlice.Dependencies.Engine
{
    public class Pools
    {
        /*static Dictionary<Type, Pool<>> pools = new Dictionary<Type, Pool<>>();

		public static Pool<Type> Get(Type type)
		{

			var pool = pools[type];

			Pool<type> p;


			if(!pools.TryGetValue(type, out p))
			{
				pool = new Pool<type>;
				pools.Add(type, pool);
			}
			return pool;
		}*/
    }
}
